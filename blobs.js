/*var n = parseInt();
var kg = parseFloat()

while (n > 0) { //complete o codigo
    var c = parseFloat();
    let dias = gets(); //atribua o valor correto a variavel
    while (kg > 1) { // complete o while
        let kg = kg / 2
        dias++
    }
}
console.log(n + ` dias`); //complete o código para a saída*/

var n = parseInt();
var kg = parseFloat(300.00)

while (n > 10) { //complete o codigo
    var c = parseFloat();
    let dias = gets(); //atribua o valor correto a variavel
    while (kg > 1) { // complete o while
        let kg = kg / 2
        dias++
    }
}
console.log(n + ` dias`); //complete o código para a saída